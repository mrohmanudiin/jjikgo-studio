{
    "name": "jjikgo-photobooth-workspace",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "dev:cashier": "npm run dev --prefix apps/cashier-app",
        "dev:staff": "npm run dev --prefix apps/staff-app",
        "dev:admin": "npm run dev --prefix apps/admin-dashboard",
        "dev:backend": "npm run dev --prefix backend",
        "build:cashier": "npm run build --prefix apps/cashier-app",
        "build:staff": "npm run build --prefix apps/staff-app",
        "build:admin": "npm run build --prefix apps/admin-dashboard"
    }
}
